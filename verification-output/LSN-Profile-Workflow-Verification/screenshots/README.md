# Screenshot evidence

The only runtime screenshot captured is 00-runtime-entry-login.jpg, showing that the Console preview loads to the authentication boundary.

The requested Superadmin, Firmware Admin, Client Reviewer, review, sandbox, publication, Windows update, and audit screenshots are NOT VERIFIED and were not fabricated. Disposable role accounts and governed profiles could not be created because users.role and all governed profile tables are absent from the development database.

The installed-Windows screenshots are additionally blocked by the absence of a Windows runner and by missing mapping-diff UI/data in the current Electron renderer bridge.
